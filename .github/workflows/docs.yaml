name: Docs

on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - 'mkdocs.yml'
      - 'flixopt/**'
  pull_request:
    paths:
      - 'docs/**'
      - 'mkdocs.yml'
  workflow_dispatch:
  workflow_call:
    inputs:
      deploy:
        type: boolean
        default: false
      version:
        type: string
        required: false

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PYTHON_VERSION: "3.11"
  MPLBACKEND: Agg
  PLOTLY_RENDERER: json

jobs:
  build:
    name: Build documentation
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - uses: astral-sh/setup-uv@v6
        with:
          version: "0.9.10"
          enable-cache: true

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Extract changelog
        run: |
          uv pip install --system packaging
          python scripts/extract_changelog.py

      - name: Install dependencies
        run: uv pip install --system ".[docs]"

      - name: Execute notebooks in parallel
        run: |
          find docs -name "*.ipynb" -print0 | xargs -0 -P 4 -I {} jupyter nbconvert --execute --inplace --to notebook {}

      - name: Build docs
        run: mkdocs build --strict
        env:
          MKDOCS_EXECUTE_NOTEBOOKS: "false"

      - uses: actions/upload-artifact@v4
        with:
          name: docs
          path: site/
          retention-days: 7

  deploy:
    name: Deploy documentation
    needs: build
    if: ${{ inputs.deploy == true }}
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - uses: astral-sh/setup-uv@v6
        with:
          version: "0.9.10"
          enable-cache: true

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install mike
        run: uv pip install --system mike

      - uses: actions/download-artifact@v4
        with:
          name: docs
          path: site/

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Deploy docs
        run: |
          VERSION=${{ inputs.version }}
          VERSION=${VERSION#v}
          mike deploy --push --update-aliases --no-build $VERSION latest
          mike set-default --push latest
